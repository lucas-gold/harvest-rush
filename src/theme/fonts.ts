// Pixelify Sans for words (titles, labels, buttons, names) to match the
// 16-bit look; numbers (scores, counts) deliberately stay in the platform's
// default font — pixel fonts get hard to read at a glance for digits you
// need to track quickly (your crop count, the leaderboard).
export const FONT_PIXEL_REGULAR = "PixelifySans_400Regular";
export const FONT_PIXEL_MEDIUM = "PixelifySans_500Medium";
export const FONT_PIXEL_SEMIBOLD = "PixelifySans_600SemiBold";
export const FONT_PIXEL_BOLD = "PixelifySans_700Bold";
